# Generated by scripts/export_openapi.py - do not hand-edit.
# Re-run that script after changing any endpoint or schema.
openapi: 3.1.0
info:
  title: Simple Chat Agent
  description: Provider-agnostic LangChain chat backend with optional Pinecone-backed
    retrieval.
  version: 1.0.0
paths:
  /api/health:
    get:
      summary: Health
      operationId: health_api_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
  /api/config:
    get:
      summary: Config
      description: 'Expose the active (non-secret) configuration so the frontend can

        show which provider/model is serving the conversation.'
      operationId: config_api_config_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigResponse'
  /api/chat:
    post:
      summary: Chat
      operationId: chat_api_chat_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChatResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/chat/stream:
    post:
      summary: Chat Stream
      operationId: chat_stream_api_chat_stream_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/reset:
    post:
      summary: Reset
      operationId: reset_api_reset_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Reset Api Reset Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
components:
  schemas:
    ChatRequest:
      properties:
        session_id:
          anyOf:
          - type: string
            maxLength: 128
          - type: 'null'
          title: Session Id
        message:
          type: string
          maxLength: 8000
          minLength: 1
          title: Message
      type: object
      required:
      - message
      title: ChatRequest
    ChatResponse:
      properties:
        session_id:
          type: string
          title: Session Id
        reply:
          type: string
          title: Reply
      type: object
      required:
      - session_id
      - reply
      title: ChatResponse
    ConfigResponse:
      properties:
        app_name:
          type: string
          title: App Name
        provider:
          type: string
          title: Provider
        model:
          type: string
          title: Model
        rag_enabled:
          type: boolean
          title: Rag Enabled
        streaming:
          type: boolean
          title: Streaming
      type: object
      required:
      - app_name
      - provider
      - model
      - rag_enabled
      - streaming
      title: ConfigResponse
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    HealthResponse:
      properties:
        status:
          type: string
          title: Status
      type: object
      required:
      - status
      title: HealthResponse
    ResetRequest:
      properties:
        session_id:
          type: string
          maxLength: 128
          minLength: 1
          title: Session Id
      type: object
      required:
      - session_id
      title: ResetRequest
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
